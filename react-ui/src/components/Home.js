import React from 'react';

const Home = (props) => {
    return (
        <section className="home">
            <nav>
                <h2>temporary home page</h2>
            </nav>
            <button>Logout</button>
        </section>
    )
}

export default Home;