import React from 'react';

const Landing = ({ userLogout }) => {

    return (
        <section className="landing">
            <nav>
                <h2>temporary landing page</h2>
            </nav>
            <button>Sign Up</button>
        </section>
    )
}

export default Landing;